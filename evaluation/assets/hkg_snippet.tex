\begin{lstlisting}[language=bash, caption={Hierarchical Knowledge Graph (HKG) embedding structural breadcrumbs into each node for context-aware retrieval.}]
[node_001] M12 male 90° A-cod. with cable
   Content: [CONTEXT: M12 male 90° A-cod. with cable]

\end{lstlisting}
